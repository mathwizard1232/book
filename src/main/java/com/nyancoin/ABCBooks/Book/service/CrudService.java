package com.nyancoin.ABCBooks.Book.service;

public interface CrudService<T> {
    // TODO implement if you feel you want the services to act similarly enough that they should all have the same pattern
    // Otherwise use the independent interfaces as they currently exist in this service subpackage
}
